The project folder is a way to quickly test this node in an local node-red
instance.

First change into the project directory and then run:

    npm install

Then start the Node-RED instance using:

    npm start

And you're up and running!
